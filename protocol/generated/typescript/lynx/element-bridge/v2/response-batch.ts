// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { ResultItem } from '../../../lynx/element-bridge/v2/result-item.js';
import { Status } from '../../../lynx/element-bridge/v2/status.js';


export class ResponseBatch {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ResponseBatch {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsResponseBatch(bb:flatbuffers.ByteBuffer, obj?:ResponseBatch):ResponseBatch {
  return (obj || new ResponseBatch()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsResponseBatch(bb:flatbuffers.ByteBuffer, obj?:ResponseBatch):ResponseBatch {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ResponseBatch()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

sessionId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

sequence():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

status():Status {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : Status.OK;
}

message():string|null
message(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
message(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

results(index: number, obj?:ResultItem):ResultItem|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? (obj || new ResultItem()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

resultsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

committed():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startResponseBatch(builder:flatbuffers.Builder) {
  builder.startObject(6);
}

static addSessionId(builder:flatbuffers.Builder, sessionId:number) {
  builder.addFieldInt32(0, sessionId, 0);
}

static addSequence(builder:flatbuffers.Builder, sequence:number) {
  builder.addFieldInt32(1, sequence, 0);
}

static addStatus(builder:flatbuffers.Builder, status:Status) {
  builder.addFieldInt16(2, status, Status.OK);
}

static addMessage(builder:flatbuffers.Builder, messageOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, messageOffset, 0);
}

static addResults(builder:flatbuffers.Builder, resultsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, resultsOffset, 0);
}

static createResultsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startResultsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addCommitted(builder:flatbuffers.Builder, committed:boolean) {
  builder.addFieldInt8(5, +committed, +false);
}

static endResponseBatch(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createResponseBatch(builder:flatbuffers.Builder, sessionId:number, sequence:number, status:Status, messageOffset:flatbuffers.Offset, resultsOffset:flatbuffers.Offset, committed:boolean):flatbuffers.Offset {
  ResponseBatch.startResponseBatch(builder);
  ResponseBatch.addSessionId(builder, sessionId);
  ResponseBatch.addSequence(builder, sequence);
  ResponseBatch.addStatus(builder, status);
  ResponseBatch.addMessage(builder, messageOffset);
  ResponseBatch.addResults(builder, resultsOffset);
  ResponseBatch.addCommitted(builder, committed);
  return ResponseBatch.endResponseBatch(builder);
}
}
