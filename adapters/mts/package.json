{
  "name": "@yew-lynx/mts-broker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:wire": "node scripts/build-wire.mjs",
    "build": "npm run generate:wire && node scripts/build-template.mjs",
    "build:wasm": "npm run generate:wire && node scripts/build-template.mjs --wasm",
    "test": "npm run generate:wire && node --test test/*.test.js"
  },
  "devDependencies": {
    "@lynx-js/tasm": "0.0.51",
    "esbuild": "0.25.9",
    "flatbuffers": "25.2.10"
  },
  "engines": {
    "node": ">=18"
  }
}
